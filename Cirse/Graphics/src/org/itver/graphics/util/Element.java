/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package org.itver.graphics.util;

/**
 *
 * @author Karo
 */
public enum Element {
    universe, head, content, version, name, creator,
    background, limits, light, object, type, src,
    width, height, deepness, thickness, appearance,
    material, texture, ambient, emissive, diffuse, 
    specular, shininess, enabled, color, position, 
    attenuation, direction, point, directional,
    angles, scale, location, rotation
    
}
